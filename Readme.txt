stealing some ideas from three.js,
a library that uses as little new c++ features as possible. 
the only reason to use c++17 is the convenience of <filesystem>.

Dependencies:
  imgui (included)  
  glfw (windowing)
  glew (shader and gl extensions)

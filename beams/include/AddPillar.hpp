#ifndef ADD_PILLAR_HPP
#define ADD_PILLAR_HPP

#include <vector>

///@return grid of support material.
std::vector<double> addPillar(std::vector<double> & arr,
  std::vector<int> & gridSize);
  
#endif